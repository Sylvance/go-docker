sudo: required
dist: trusty
python: "3.6"
services:
  - docker
env:
  global:
    - IMAGE_NAME=sylvance/kuns-orbit
    - secret: "my-secret-squared"

before_script:
  - docker pull "registry.heroku.com/${IMAGE_NAME}:latest" || true
script:
  - docker build --pull --cache-from "registry.heroku.com/${IMAGE_NAME}:latest" --tag "registry.heroku.com/$IMAGE_NAME" .

after_script:
  - docker images

before_deploy:
  - docker login --username=_ --password=${HEROKU_AUTH_TOKEN} registry.heroku.com
  - git_sha="$(git rev-parse --short HEAD)"
  - docker tag "$IMAGE_NAME" "registry.heroku.com/${IMAGE_NAME}:latest"
  - docker tag "$IMAGE_NAME" "registry.heroku.com/${IMAGE_NAME}:${git_sha}"
deploy:
  - provider: script
    script: docker push "registry.heroku.com/${IMAGE_NAME}:latest"
    on:
      branch: master

  - provider: script
    script: docker push "registry.heroku.com/${IMAGE_NAME}:${git_sha}"
    on:
      branch: develop
